# SKILLS.md

## Installed skill set (Ads Optimizer)
- `bid_budget_manager`
- `keyword_opportunity_hunter`
- `negative_keyword_guard`

Use these skills in sequence:
1. stabilize loss channels (`negative_keyword_guard`)
2. rebalance spend (`bid_budget_manager`)
3. expand profitable demand (`keyword_opportunity_hunter`)

All outputs must be proposal payloads for `actionType=optimize_ads`.

